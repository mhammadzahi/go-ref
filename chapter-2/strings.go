package main

import "fmt"

func main() { //rename it
	// myStr := "Hello world"
	// for i := 0; i < len(myStr); i++ {
	// 	char := myStr[i]
	// 	fmt.Printf("%c\n", char)
	// }
	myStr := "hello"
	fmt.Printf("%s\n", myStr)
}
